import 'package:flutter/material.dart';

class ConsultasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Consultas"),
      ),
      body: Center(
        child: Text("Página para realizar consultas."),
      ),
    );
  }
}